export default function RootLayout({ children }) {
    return (
      <html lang="en">
        <head>
          <title>Responsive Layout</title>
        </head>
        <body>{children}</body>
      </html>
    );
  }
  